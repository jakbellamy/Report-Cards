import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  root: {
    // paddingTop: '',
  },
  card: {
    marginTop: '2.5%'
  },
  title: {
    textAlign: 'center',
  },
  subtitle: {
    textAlign: 'left',
    paddingLeft: '2.5%'
  }
}));
